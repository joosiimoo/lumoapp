# Lumo mobile

Flutter client for Build A. Local API base URL is `http://127.0.0.1:8000` (Compose `api` service).

```bash
cd mobile
flutter pub get
flutter test
flutter run --dart-define=LUMO_ENV=local
```
