## 1. Domain

- [x] 1.1 Add `CashCount` and `CashCountSource` (`manual_capture` only) under `backend/app/domain/operations/cash_count.py`. Fields: `id`, `business_id`, `operational_day_id`, `actor_id`, `amount` (`Decimal`), `currency`, `source`, `counted_at`, `supersedes_cash_count_id`, `superseded_by_id`, `created_at`, `updated_at`. Frozen dataclass. Domain MUST NOT import SQLAlchemy, FastAPI, or Flutter. Export it from `app.domain.operations`.
- [x] 1.2 Add `CashStatus` (`not_counted`, `balanced`, `over`, `short`) and a pure `cash_difference(expected, counted) -> Decimal | None` plus `cash_status_for(difference)` in the same package. Quantize to two places, keep the sign, and return `None` / `not_counted` when `counted` is `None`. No tolerance or rounding band.
- [x] 1.3 Add `parse_counted_amount(raw) -> Decimal` validation: non-negative, at most two fraction digits, `,` normalized to `.`, no thousands separators, rejects `-1`, `1.005`, `abc`, and empty. Unit-test each case. Reject binary float input.

## 2. Persistence and migration

- [x] 2.1 Alembic file `0006_cash_count.py`, revision id `0006_cash_count`, `down_revision = "0005_operational_day"`. Create `operations.cash_counts` with the columns of design §2, CHECK `amount >= 0`, CHECK `source IN ('manual_capture')`, the two self-reference CHECKs, and the composite FK `(operational_day_id, business_id)` → `operations.operational_days (id, business_id)`. Do not declare the self-referencing FKs in `create_table`. No backfill.
- [x] 2.2 In the same migration add `UNIQUE (id, business_id)`, `UNIQUE (supersedes_cash_count_id)`, `UNIQUE (superseded_by_id)`, partial unique index `uq_cash_counts_current` on `(operational_day_id) WHERE superseded_by_id IS NULL`, indexes on `business_id` and `operational_day_id`, ENABLE + FORCE RLS, policy `tenant_isolation`, and `lumo_app` DML grants matching `operational_days`. Do not alter `operational_days`, `sale_sessions`, or `payments`. Do not create `workflow`, `memory`, `closing_snapshots`, `work_items`, or `export_jobs`.
- [x] 2.3 After `UNIQUE (id, business_id)` exists, add the two tenant-safe composite self-referencing FKs with separate `op.create_foreign_key` calls, both targeting `operations.cash_counts (id, business_id)`: `fk_cash_counts_supersedes` on `(supersedes_cash_count_id, business_id)` immediate, and `fk_cash_counts_superseded_by` on `(superseded_by_id, business_id)` with `deferrable=True, initially="DEFERRED"`. Never reference `id` alone. Leave the partial unique index and both supersede unique constraints immediate.
- [x] 2.4 Write the downgrade: drop `operations.cash_counts` with its indexes, policy, and constraints only. Keep schema `operations`, `operational_days`, sessions, items, and payments untouched. Verify a successful upgrade still leaves FORCE RLS enabled.
- [x] 2.5 Add `CashCountRow` to `backend/app/infrastructure/persistence/models.py` mirroring the table exactly: constraints, partial index, `{"schema": "operations"}`, and both composite `ForeignKeyConstraint`s with `deferrable=True, initially="DEFERRED"` on the `superseded_by_id` one only. Assert in a test that the model metadata and the migrated database agree on the deferral.

## 3. Repository and expected cash

- [x] 3.1 Extend `OperationsRepository` with `lock_day_for_update(tenant, business_date) -> OperationalDay | None` using `SELECT … FOR UPDATE` on `operations.operational_days` for `(business_id, business_date)`. Return `None` without inserting when the day does not exist.
- [x] 3.2 Add `expected_cash(tenant, operational_day_id, currency) -> str` that reuses the existing `summarize_day` aggregation so `expected_cash` always equals that day's `cash_total`. Confirmed sessions and `recorded` payments only, `cash` method only, Decimal, quantized, `0.00` when empty. Do not sum `line_total`s and do not add a second divergent SQL path.
- [x] 3.3 Add `get_current_cash_count(tenant, operational_day_id)` (`superseded_by_id IS NULL`), `mark_superseded(tenant, previous_id, new_id)`, and `insert_cash_count(...)` that accepts a caller-supplied `id`. `mark_superseded` MUST be callable before the new row exists and MUST issue the `UPDATE` immediately (explicit flush), because the recount retires the previous row first. Every method MUST require a tenant, scope its `WHERE` by `business_id`, and set the RLS business id. Copy `business_id` and `actor_id` from `TenantContext`.

## 4. Cash count write workflow

- [x] 4.1 Add `backend/app/application/workflows/record_cash_count.py` with `operation_type = "lumo.message.record_cash_count"`. Implement steps A–L of design §7 in that exact order: peek idempotency → read business (timezone, currency) → one injectable UTC clock reading as `counted_at` → `business_date_for` → lock the day → clarify when absent → compute expected cash under the lock → read the current count. An invalid IANA timezone MUST raise so `get_db` rolls back.
- [x] 4.2 No day for today: return a non-mutating clarify result (`CLOSE-001`, reason `operational_day_not_started`) with Spanish copy that no sales are registered today. Assert in tests that no cash count, day, audit, outbox, or idempotency row is written.
- [x] 4.3 Idempotency step A uses the existing `IdempotencyService.peek` **before** the day lock: a completed record with the same hash returns the stored body verbatim and returns early; a different hash raises `IdempotencyConflictError`; absent, processing, or failed falls through. Call `begin` only at step G, after the equal-amount read-back check has been ruled out, so an equal-amount read-back leaves no idempotency row. Add a unit test asserting `begin` is not called on the read-back path.
- [x] 4.4 Perform the write in the order required by design §3. First count: one insert with both supersede columns NULL. Recount: generate `new_uuid7()` first, `UPDATE previous.superseded_by_id = new_id`, then `INSERT` the new row with `supersedes_cash_count_id = previous.id` and `superseded_by_id = NULL`. Flush in that statement order (do not let the ORM reorder insert before update) so the immediate partial unique index never sees two current rows; the deferred FK is validated at commit. Tenant `business_id`/`actor_id`, business currency, `source=manual_capture`, `counted_at` from step C.
- [x] 4.5 Audit `closing.submit_cash_count@1` with `before_payload` (previous count id and amount, or null) and `after_payload` (`cash_count_id`, `operational_day_id`, `business_date`, `amount`, `currency`, `expected_cash`, `cash_difference`, `cash_status`, `counted_at`, `supersedes_cash_count_id`). Enqueue exactly one `cash_count.recorded` outbox event. Complete the idempotency record. Compose the response only after commit.
- [x] 4.6 Confirm the workflow never inserts or updates a `SaleSession`, `SaleItem`, `Payment`, or `OperationalDay`, and never changes `OperationalDay.status`.

## 5. Close-preparation read workflow

- [x] 5.1 Add `backend/app/application/workflows/get_daily_close_preparation.py`: today's business date from the same clock and timezone rule, `get_by_date` without locking, expected cash from task 3.2, current count from task 3.3, then derived difference and status from tasks 1.2. Reuse `sale_count` from the existing day aggregation.
- [x] 5.2 Build the payload of design §6 (`operational_day_id`, `business_date`, `day_status`, `currency`, `sale_count`, `expected_cash`, `counted_cash`, `cash_difference`, `cash_status`, `counted_at`, `cash_count_id`) plus `text`. Omit `gross_sales_total`, `card_total`, and `transfer_total`.
- [x] 5.3 No day: return the not-started payload (`sale_count=0`, `expected_cash=0.00`, nulls, `cash_status=not_counted`, today's business date) without inserting. Assert the read writes no day, cash count, audit, outbox, or idempotency row, and that a repeat read is stable.

## 6. Tools, policies, and interpreter

- [x] 6.1 Register `closing.submit_cash_count@1`: input schema with `amount` as the only property, output = preparation payload plus `cash_count_id` and `supersedes_cash_count_id`, permission `closing.submit_cash_count`, policy `CLOSE-001`, `side_effect=write`, `requires_idempotency=true`.
- [x] 6.2 Register `closing.prepare@1`: empty input object, preparation output, permission `closing.submit_cash_count`, policy `CLOSE-002`, `side_effect=read`, `requires_idempotency=false`. Do not register `closing.confirm`, `closing.reopen`, `export.*`, or `operational_day.get`.
- [x] 6.3 Add `CLOSE-001` and `CLOSE-002` to `FoundationPolicyEngine` with the reason codes of design §12, keeping the documented evaluation order. `CLOSE-001` clarifies with `operational_day_not_started`. Neither policy may accept model-supplied expected cash, counted cash, difference, status, day id, or date. Keep `SEC-002` denying `closing.confirm@1` and `closing.reopen@1`.
- [x] 6.4 Add optional `counted_amount` to `AgentDecision` with intents `record_cash_count` and `close_preparation`. Do not add expected-cash, difference, status, day-id, or date fields.
- [x] 6.5 Teach the scripted interpreter the cash-count grammar (`tengo <amount> en caja`, `hay <amount> en caja`, `conte <amount>`, `caja <amount>`) with the amount pattern of design §11, matching after payment, totalize, and day-summary and before product parsing. Normalize `,` to `.`. Reject thousands separators and a bare amount. No repository access.
- [x] 6.6 Teach the interpreter the preparation set (`preparar el cierre`, `preparar cierre`, `cuanto deberia haber en caja`, `efectivo esperado`) and the no-close guard: `cerrar el dia`, `cerrar la jornada`, `cerrar caja`, `confirmar cierre` clarify with copy that says the close can be prepared and cash compared, and that confirming the close is not available yet. No tool selection, no mutation.
- [x] 6.7 Wire the orchestrator and `POST /api/v1/lumo/messages`: `record_cash_count` → write workflow, `close_preparation` → read workflow. Neither may run add-item, totalize, commit, or the day summary. The orchestrator must not open a transaction or compute cash values.

## 7. Generative UI backend

- [x] 7.1 Register `daily_close_preparation@1` with the data schema of design §13 and `actions=[]`. Compose it for both the completed write and the completed read, including the not-started and not-counted payloads. `text` MUST equal `fallback_text`.
- [x] 7.2 Build `fallback_text` with status words `Sin contar`, `Caja cuadrada`, `Sobrante`, `Faltante`, server decimal strings, a rendered negative sign, and the `Falta contar efectivo` sentence when `cash_status=not_counted` (no counted amount, no difference). Refuse `closing_ready_card@1` and `cash_difference_card@1`.

## 8. Flutter

- [x] 8.1 Register `daily_close_preparation` version `1` in `GenerativeUIRenderer.known` and add the render branch. Unknown version shows `fallback_text` and runs no action.
- [x] 8.2 Build the card with existing primitives only: Lumo-mark gutter, `LumoCard`, metric rows for `EFECTIVO ESPERADO`, `CONTADO`, `DIFERENCIA`, and a status chip from `cash_status`. Show "Falta contar efectivo" for `not_counted` with no counted amount and no difference. Format server strings only: no subtraction, comparison, status derivation, or device-timezone date math. No confirm-close button, reopen, exceptions, chart, or history.
- [x] 8.3 Keep the Inicio `conversation_id` stable across cash-count and preparation sends. Leave Hoy, Memoria, and Negocio placeholders and leave "Preparar el cierre del día" unwired. Do not add a closing screen, theme, accent, motion, or skeleton.

## 9. Integrity, config, and ADR

- [x] 9.1 Extend `backend/tests/sale_cleanup.py`: add `closing.submit_cash_count@1` to the audit actions, `cash_count.recorded` to the outbox events, `lumo.message.record_cash_count` to the message operations, and delete `operations.cash_counts` for the tenant before `operations.operational_days` in the same transaction.
- [x] 9.2 Extend `sale_integrity_orphans`: flag a cash count with a missing `operational_day_id`, a broken `supersedes_cash_count_id` or `superseded_by_id` link, a supersede link whose target row has a different `business_id`, more than one current count per day, and audit or outbox rows referencing a missing `cash_count_id` or `operational_day_id`.
- [x] 9.3 Update `openspec/config.yaml` context: `operations` holds `operational_days` and `cash_counts`; tools add `closing.submit_cash_count@1` and `closing.prepare@1`; UI adds `daily_close_preparation@1`; `workflow` and `memory` stay absent; `OperationalDay.status` is still `open` only; Daily Close confirmation is still not implemented.
- [x] 9.4 Write `docs/adr/ADR-017-cash-count-and-close-preparation.md` per design §17: expected cash formula with no opening float, append-only counts with one current row, tenant-safe composite self-references with one deferred FK and the update-then-insert recount order, derived difference and status, `closing.submit_cash_count@1` write and `closing.prepare@1` read, count requires an existing day, `status` stays `open`, `closing.confirm` out of scope. Do not rewrite ADR-015 or ADR-016.
- [x] 9.5 Spec repair only, no code: confirm the implemented `sale.commit@1` already ensures and attaches the `OperationalDay` and creates no `CashCount`, `WorkItem`, invoice, or Daily Close, so the `conversational-sale-session` delta matches behavior. Do not modify `commit_sale`, its transaction, its audit, or its events. If any assertion fails, stop and report instead of changing sale behavior in this change.

## 10. Acceptance tests

- [x] 10.1 Scenario 1 — no sales, no day: `preparar el cierre` returns the not-started payload (`sale_count=0`, `expected_cash=0.00`, nulls, `not_counted`) and inserts no day and no count; `tengo 120 en caja` clarifies and writes nothing.
- [x] 10.2 Scenario 2 — cash sales, no count: one confirmed cash sale of `22.50` gives `expected_cash=22.50`, `counted_cash=null`, `cash_difference=null`, `cash_status=not_counted`.
- [x] 10.3 Scenario 3 — balanced: count `22.50` against expected `22.50` gives `0.00` and `balanced`.
- [x] 10.4 Scenario 4 — shortage: count `20.00` against expected `22.50` gives `-2.50` and `short`.
- [x] 10.5 Scenario 5 — overage: count `25.00` against expected `22.50` gives `2.50` and `over`.
- [x] 10.6 Scenario 6 — method isolation: cash `22.50`, card `10.00`, transfer `24.00` on one business date leaves `expected_cash=22.50`, equal to that day's `cash_total` from `operational_day.summary@1`.
- [x] 10.7 Scenario 7 — recount: count `20.00` then `22.50`; the current count is `22.50` with `balanced`, the `20.00` row is superseded with its original amount and `counted_at`, and two audit plus two outbox rows exist.
- [x] 10.8 Recount ordering: with a current count of `20.00`, a recount of `22.50` commits despite the immediate partial unique index; assert `previous.superseded_by_id == new.id`, `new.supersedes_cash_count_id == previous.id`, `new.superseded_by_id IS NULL`, and exactly one row for the day with `superseded_by_id IS NULL`. Add a statement-order assertion (SQL capture or repository spy) that the `UPDATE` precedes the `INSERT`.
- [x] 10.9 Recount rollback between retire and insert: force a failure after the previous row is retired (`X-Debug-Fail-After-Write` or an injected error); assert rollback alone restores `previous.superseded_by_id IS NULL`, the previous row is current with its original `amount` and `counted_at`, the new count id does not exist, and no audit, outbox, or completed idempotency row remains. No compensating update may exist in the code path.
- [x] 10.10 Deferred FK validity: after a successful recount commit, assert every `superseded_by_id` and `supersedes_cash_count_id` resolves to an existing row with the same `business_id`; and assert a transaction that sets `superseded_by_id` to an id that is never inserted fails at `COMMIT`, not at the `UPDATE`.
- [x] 10.11 Cross-tenant chain: with a `CashCount` in Carrota and one in business B, assert a direct SQL attempt to set either supersede column across tenants is rejected by the composite foreign key, run with a context where RLS is not the blocking factor so the failure is attributable to the constraint; assert `sale_integrity_orphans` reports no cross-tenant links after the normal flows.
- [x] 10.12 Scenario 8 — same-key replay: resubmitting the same count with the same key and hash returns the original body, leaves one row, and adds no second outbox row. Same key with a different amount returns `IDEMPOTENCY_CONFLICT` and changes nothing.
- [x] 10.13 Replay is stale, new-key read-back is live: record `20.00` against expected `20.00`, confirm another cash sale of `2.50`, then resubmit `20.00` twice. Assert the same-key replay returns the stored original body (`expected_cash 20.00`, `0.00`, `balanced`), the new-key submission returns live values (`expected_cash 22.50`, `-2.50`, `short`), one `CashCount` exists, and exactly one `lumo.message.record_cash_count` record exists — none for the new key.
- [x] 10.14 Idempotency matrix: assert row-count deltas for all four cases — same key/same hash (no new rows), same key/different amount (conflict, no new rows), different key/equal amount (no idempotency row, no cash count, no audit, no outbox), different key/different amount (one cash count, one audit, one outbox, one completed idempotency record).
- [x] 10.15 Scenario 9 — concurrency: two concurrent writes with different keys and different amounts leave exactly one current count, a linear supersede chain containing both amounts, and no lost update; a new key with an amount equal to the current count is a read-back with no new row.
- [x] 10.16 Scenario 10 — RLS: business B cannot read Carrota's `cash_count_id`, amount, or difference, and cannot write a count against Carrota's `operational_day_id`.
- [x] 10.17 Scenario 11 — rollback: a forced failure after the cash-count write leaves the previous current count unchanged with no new row, audit, or outbox row, and a retry with the same key then succeeds exactly once.
- [x] 10.18 Scenario 12 — no client math: Flutter tests assert the card renders server `expected_cash`, `counted_cash`, `cash_difference`, and `cash_status`, never subtracting or deriving status, and render "Falta contar efectivo" for the not-counted payload.
- [x] 10.19 Scenario 13 — unsupported phrases: `tengo 1,200 en caja`, `120`, and `cuánto falta en caja la semana pasada` clarify with no cash count, no day, and no card.
- [x] 10.20 Scenario 14 — no final close: `cerrar el día` and `confirmar cierre` clarify; `closing.confirm@1` stays unregistered and denied under `SEC-002`; no `closed` status, close event, `ClosingSnapshot`, WorkItem, or OutcomeRun exists after the full flow.
- [x] 10.21 Commit still attaches the day and counts no cash (spec-repair regression): confirming the first cash sale of the date creates exactly one open `OperationalDay`, sets `operational_day_id`, and leaves `operations.cash_counts` empty with `status` `open`.
- [x] 10.22 Migration test: from revision `0005_operational_day` with existing confirmed sales, payments, and operational days, `0006_cash_count` upgrades to head, leaves those rows unchanged, creates `operations.cash_counts` with FORCE RLS, the `tenant_isolation` policy, and `uq_cash_counts_current`, and rejects a second current count and a negative amount. Downgrade drops only that table and keeps `operational_days` and the sales rows.
- [x] 10.23 Migration constraint introspection: query `pg_constraint` to assert both supersede foreign keys reference `(id, business_id)`, that `fk_cash_counts_superseded_by` has `condeferrable` and `condeferred` true, that `fk_cash_counts_supersedes` has both false, and that `uq_cash_counts_current` is a partial unique index with the `superseded_by_id IS NULL` predicate.
